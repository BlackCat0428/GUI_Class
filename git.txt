git clone https://github.com/BlackCat0428/GUI_Class
	拉檔案

git add .
	向Local git 新增所有檔案

git config --global user.email "ries3330@gmail.com"

git config --global user.name "BlackCat"
	設定使用者帳戶資訊

git commit –m "註解"
	標記該次Push的註解

git push -u origin main
	將程式碼Push到GitHub上


git clone https://github.com/BlackCat0428/GUI_Class

git add .

git config --global user.email "ries3330@gmail.com"


git config --global user.name "BlackCat"

git commit –m "註解"

git push -u origin main